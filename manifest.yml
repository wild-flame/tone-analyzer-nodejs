---
declared-services:
  tone-analyzer-service-experimental:
    label: tone_analyzer
    plan: experimental
applications:
- services:
  - tone-analyzer-service
  name: tone-analyzer-demo-wildflame
  command: node app.js
  path: .
  memory: 512MB
  env:
    NODE_ENV: production
